---
import { getImage } from 'astro:assets';
import type { ImageMetadata } from 'astro';
import BaseLayout from '../../layouts/BaseLayout.astro';
import PageSeo from '../../components/seo/PageSeo.astro';
import { standalonePages } from '../../../site-pages';
import Work from '@/components/Work.vue';

const page = standalonePages.work;

// ---------------------------------------------------------------------------
// Avif pour le ReachOutCTA en bas de la page work
// ---------------------------------------------------------------------------
const workGlob = import.meta.glob<{ default: ImageMetadata }>(
  '/src/assets/media-src/pages/work/*.jpg',
  { eager: true },
);

let reachOutAvifSrcset = '';
const reachOutSrc =
  workGlob[
    '/src/assets/media-src/pages/work/andrej-lisakov-S13Sj0d-r60-unsplash.jpg'
  ]?.default;
if (reachOutSrc) {
  const result = await getImage({ src: reachOutSrc, widths: [480, 640, 960, 1280], format: 'avif' });
  reachOutAvifSrcset = result.srcSet.attribute;
}
---

<BaseLayout>
  <PageSeo slot="head" title={page.title} description={page.description} />
  <Work client:load reachOutAvifSrcset={reachOutAvifSrcset} />
</BaseLayout>
